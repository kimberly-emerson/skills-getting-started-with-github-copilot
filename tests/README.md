# FastAPI Backend Tests

## How to run

1. Install dependencies:
   ```
pip install -r ../requirements.txt
   ```
2. Run tests:
   ```
pytest
   ```

## Notes
- Tests use FastAPI's TestClient for API endpoint testing.
- Add more tests in this directory as needed.
